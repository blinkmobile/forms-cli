# Forms AngularJS 1.5+ Plugin

A plugin for the Blink Mobile Technologies Forms definition transformer.

## Usage

via selecting `AngularJS` during `bm forms init` or manually:


```
cd path/to/project
bm forms plugin add AngularJS
```
